import { NodeDisplayDirective } from './node-display.directive';

describe('NodeDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new NodeDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
